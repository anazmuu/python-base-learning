#tuple adalah objek python yang tidak dapat berubah
#tuple adalah urutan, seperti daftar
#tuple tidak dapat dirubah seperti list

hewan = ("Ayam", "Kucing") #Data tidak bisa di rubah
            #0      #1
angka = (2, 4, 6, 8, 10)

print(hewan)
print(hewan[1]) #pemanggilan secara elemen

print(len(hewan)) #length tuple
print(max(angka))
print(min(angka))